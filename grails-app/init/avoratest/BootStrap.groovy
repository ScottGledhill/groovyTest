package avoratest

import org.avora.Metric
import org.avora.Report
import org.avora.Sales
import org.avora.User
import org.avora.Visits

class BootStrap {

    // def init = { servletContext ->

    // def sales = new Sales(dateCreated: new Date(), amount:100, tax: 20, description:"shoes",email:"john@avora.com").save(failOnError:true,)
    //     def visits = new Visits(email:"john@avora.com", date:new Date(), duration:10).save(failOnError:true)

    //     def user = new User(email:"john@avora.com", name:"john smith", location:"West Midlands").save(failOnError:true)
    //     def user2 = new User(email:"manjit@google.com", name:"manjit johal", location:"Berkshire").save(failOnError:true)
    //     def user3 = new User(email:"jane@avora.com", name:"Jane Jones", location:"Reading").save(failOnError:true)

    //     def salesMetric = new Metric(name: "Total Sales", query:"sales").save(failOnError:true)
    //     def salesReport = new Report(name: "All Sales", user:user)
    //     salesReport.addToMetrics(salesMetric)
    //     salesReport.save(failOnError:true)
    // }
    // def destroy = {
    // }
}